
console.log('no code yet!')

const state =  { }

const peInit = n => state.name = n

const peUpdate = () => {
    for (div of document.getElementsByTagName("div")) {
        let pe = element.getAttribute('pe');
console.log('pe: ',pe)
        if (pe != '') {
            div.style.border = "3px solid orange"
        }
    }
}